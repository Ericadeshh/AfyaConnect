Great, now for the next step, I need you to handle the payments done in the following ways:
1. create a payment status page, such that:
-If payment is successful, a successful pop-up is displayed, and payment referral details are shown. These details should include (patient's name/ID, amount, receiving facility, sending facility(where the referral is made, time, other important details)
-a payment is not successful(wrongly made, canceled, insufficient amount on end-user), then return to the payment page. 

2. create a payment history dashboard, ensure it comprehensive in the following ways:
-Both physician and admin can access it, 
-should display all the details of the referral transaction, from the successful referral feedback and other important information that are of relevance,
-use stats, cards, a chart, maybe a figure or medical related analysis metrics that are of importance and relevance,
-be simple, modern, aesthetic and smooth,

I am going to send files for Physician and Admin, which will be quite a long list, but you should analyze each one of them in order to create the two requests above.

ON THE PHYSICIAN:
Here's what to take note of:
-The user/person in-charge of making referrals is the PHYISICIAN ONLY. 
-So I am going to provide files for physician, then you will update as follows:
(i) Inspect the referral creation form to find out where the service for making payment( section with a button) should be placed. In my opinion, it should come after the referral details have been created, as the last step then referral process is complete.
(ii)analyze the logic in which the physician's user character is implemented, how the convex and src files are setup to enable communication between backend and frontend, allowing real-time interaction with the convex database,  to fetch/display/process data between frontend and backend smoothly-this logic and implementation should be followed to create the requests above.
(iii) You can add information on the pages that there is safaricom mobile payment service.

ON THE ADMIN:
Here's what to take note of:
-Should have access of the payment history page also.
(i) Create an Interface to access payment history, such that it is part of the new tabs, and also in quick actions, it should be easily accessible.




Excellent approach! Let's break this down into incremental, testable steps. Each step will build on the previous one and we can verify everything works before moving forward.

📋 Step-by-Step Implementation Plan
Step 1: Database Updates ✅ Foundation
First, let's update the schema to link payments with referrals.

Files to create/update:

convex/schema.ts (update payments table)

What we'll verify:

Schema updates are valid

New indexes are created

No conflicts with existing data

Step 2: New Convex Queries 📊 Data Layer
Create the necessary queries to fetch payment data with referral details.

Files to create:

convex/payments/queries.ts (new file)

convex/payments/analytics.ts (new file)

What we'll verify:

Can fetch payment with referral details

Can get physician's payment history

Can get admin payment analytics

All queries work in Convex dashboard

Step 3: Update Payment Mutation 🔄 Payment-Referral Linking
Modify the payment initiation to link payments to referrals.

Files to update:

convex/payments.ts (update initiateSTKPush action)

src/lib/payment.ts (update sendSTKPaymentPrompt)

What we'll verify:

Payment is created with referralId

STK push still works

Callback updates correct payment

Step 4: Payment Status Page 🎯 UI Component 1
Create the payment status page with success/failure handling.

Files to create:

src/components/payments/PaymentStatusPage.tsx

src/app/payment/status/[paymentId]/page.tsx (if using App Router)

What we'll verify:

Success page shows correct referral details

Failure page shows error and retry option

Polling for status updates works

Navigation flow is smooth

Step 5: Physician Integration 👨‍⚕️ Add Payment to Referral Flow
Add payment as the final step in physician referral creation.

Files to update:

src/components/physician/create-referral.tsx (add Step 5)

Create src/components/physician/ReferralPaymentStep.tsx

What we'll verify:

After referral creation, payment step appears

Payment amount is correct (REFERRAL_FEE)

Phone number input is pre-filled/validated

On success, redirects to status page

On failure, stays on payment step with error

Step 6: Physician Payment History 👨‍⚕️📋 Dashboard Integration
Add payment history to physician dashboard.

Files to create:

src/components/physician/PaymentHistory.tsx

Update src/components/physician/PhysicianDashboard.tsx (add to quick actions)

What we'll verify:

Shows all payments from physician's referrals

Displays patient name, amount, date, status

Can filter by status

Links to referral details

Responsive design works

Step 7: Admin Payment Analytics 👨‍💼📊 Admin Dashboard Integration
Add comprehensive payment analytics to admin dashboard.

Files to create:

src/components/admin/AdminPaymentAnalytics.tsx

src/components/admin/AdminPaymentHistory.tsx

Update src/components/admin/AdminDashboard.tsx (add Payments tab)

What we'll verify:

Stats cards show correct totals

Charts render with real data

Payment history table works

Filters and search work

All data updates in real-time

Step 8: Polish & Testing ✨ Final Touches
Add loading states, error boundaries, and final polish.

Files to update:

All new components (add skeletons, loading states)

Add error boundaries

Add proper TypeScript types

Add comments for complex logic

What we'll verify:

Loading states show correctly

Errors are handled gracefully

All TypeScript errors resolved

Responsive on all screen sizes

Blue theme consistent throughout

🎯 Testing Checklist Per Step
For each step, we'll verify:

No console errors in browser/dev tools

TypeScript compilation passes

Convex functions run without errors

UI renders correctly on mobile/desktop

Data flow works end-to-end

🚀 Ready to Start
Which step should we begin with? I recommend Step 1 (Database Updates) as it's the foundation, but if you want to see UI first, we could start with Step 4 (Payment Status Page) using mock data.

Please let me know which step you'd like to tackle first, and I'll provide the complete code for that step.